<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RecuperacionSC.ViewModel"
             x:Class="RecuperacionSC.Views.DetallePersonajePage">

    <ScrollView>
        <StackLayout Padding="20" Spacing="15">
            <Image Source="{Binding Image}" HeightRequest="250" Aspect="AspectFit"/>
            <Label Text="{Binding Name}" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>
            <Label Text="{Binding Race}" FontSize="18" HorizontalOptions="Center"/>
            <Label Text="{Binding Gender}" FontSize="18" HorizontalOptions="Center"/>
            <Label Text="Descripción:" FontSize="20" FontAttributes="Bold"/>
            <Label Text="{Binding Description}" FontSize="16" TextColor="Gray"/>
        </StackLayout>
    </ScrollView>

</ContentPage>
