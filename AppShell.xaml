<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RecuparcionSC.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:RecuperacionSC.Views;assembly=RecuperacionSC"
    Shell.FlyoutBehavior="Disabled"
    Title="RecuperacionSC">

    <!-- Definir las pestañas de navegación -->
    <TabBar>

        <!-- Pestaña para la página principal -->
        <ShellContent Title="Inicio"
                      ContentTemplate="{DataTemplate views:ListaPersonajesPage}" />
    </TabBar>

</Shell>
